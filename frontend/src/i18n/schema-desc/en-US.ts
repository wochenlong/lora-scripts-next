export default {
  " dylora 分割块数单位，最小 1 也最慢。一般 4、8、12、16 这几个选": " DyLoRA split-block unit; minimum 1 is also the slowest. Common choices: 4, 8, 12, 16",
  "(可选) VAE 模型文件路径，使用外置 VAE 文件覆盖模型内本身的": "(Optional) VAE model file path; use an external VAE file to override the one bundled with the model",
  "**危险** 自定义参数，请输入 TOML 格式，将会直接覆盖当前界面内任何参数。实时更新建议写完后再粘贴过来": "**Danger** Custom parameters in TOML format; they directly override any parameter in this UI. Updates apply in real time — finish writing before pasting",
  "**危险** 自定义参数，请输入 TOML 格式，将会直接覆盖当前界面内任何参数。实时更新，推荐写完后再粘贴过来": "**Danger** Custom parameters in TOML format; they directly override any parameter in this UI. Updates apply in real time — it's recommended to finish writing before pasting",
  "AE 模型文件路径": "AE model file path",
  "AdaLN 调制层学习率": "AdaLN modulation layer learning rate",
  "Anima Fast 参数": "Anima Fast parameters",
  "Anima JSON 标签说明": "Anima JSON tag notes",
  "Anima LoRA cache 目录；不填时自动使用 .cache/anima_fast": "Anima LoRA cache directory; defaults to .cache/anima_fast when empty",
  "Anima 专用参数": "Anima-specific parameters",
  "Anima 主 DiT / transformer 权重路径": "Anima main DiT / transformer weights path",
  "Anima 主 DiT / transformer 权重路径（全量微调将更新此 DiT 权重）": "Anima main DiT / transformer weights path (full fine-tuning updates these DiT weights)",
  "Anima 主 DiT / transformer 权重路径，例如 anima-base-v1.0.safetensors": "Anima main DiT / transformer weights path, e.g. anima-base-v1.0.safetensors",
  "Anima 分桶 token 上限预检查": "Anima bucket token limit precheck",
  "Anima 分桶 token 上限预检查。0 表示不限制": "Anima bucket token limit precheck. 0 means no limit",
  "Anima 原图目录；不填时使用「训练图片目录」": "Anima source image directory; the \"Training Image Directory\" is used when empty",
  "Anima 训练预览采样器": "Anima training preview sampler",
  "Anima 训练预览采样器（当前为内置 Rectified Flow Euler 预览）": "Anima training preview sampler (currently the built-in Rectified Flow Euler preview)",
  "Anima 预览调度器": "Anima preview scheduler",
  "Attention 加速实现。留空 = 自动选择最优方案": "Attention acceleration implementation. Leave empty = automatically select the best option",
  "Attention 加速实现。留空 = 自动选择最优方案（优先 Flash Attention 2，其次 xformers，最后 PyTorch SDPA）。推荐留空让系统自动检测。flash 需要安装 flash-attn 且显卡算力≥8.0（RTX 30系+）": "Attention acceleration implementation. Leave empty = automatically select the best option (Flash Attention 2 first, then xformers, finally PyTorch SDPA). Leaving it empty is recommended to let the system auto-detect. flash requires flash-attn installed and GPU compute capability ≥ 8.0 (RTX 30 series or newer)",
  "Attention 加速实现。留空使用 torch 保底，避免 flash-attn 缺失导致预检查失败；手动选择 flash 需要插件环境已安装 flash-attn 且显卡支持": "Attention acceleration implementation. Leave empty to fall back to torch, avoiding precheck failures when flash-attn is missing; selecting flash manually requires flash-attn installed in the plugin environment and GPU support",
  "CFG Scale": "CFG Scale",
  "CFG Scale。Anima 官方建议 4-5": "CFG Scale. Anima officially recommends 4-5",
  "CFG Scale（Anima 建议 4–5）": "CFG Scale (Anima recommends 4–5)",
  "CFG Scale（turbo_dit 采样时自动按 1 处理）": "CFG Scale (automatically treated as 1 when sampling with turbo_dit)",
  "CFG 引导缩放": "CFG guidance scale",
  "CFG 引导缩放（Krea 2 t2i 训练时通常不使用）": "CFG guidance scale (typically unused for Krea 2 t2i training)",
  "CFG 截断": "CFG truncation",
  "CLIP 跳过层数 *玄学*": "CLIP skip layers *voodoo*",
  "DataLoader worker 数": "Number of DataLoader workers",
  "DataLoader worker 数。Anima 默认 0，避免 Windows 多进程启动时重复打印 Triton 警告": "Number of DataLoader workers. Anima defaults to 0 to avoid duplicate Triton warnings during Windows multiprocess startup",
  "Delta FM 损失中对比项的权重": "Weight of the contrastive term in the Delta FM loss",
  "DiT 全量微调基础学习率（对应上游 anima_train.py --learning_rate）": "Base learning rate for DiT full fine-tuning (corresponds to upstream anima_train.py --learning_rate)",
  "DiT 基础权重使用 fp8，大幅降低显存占用。须与 fp8_scaled 同时开启（默认都开）；只开一个会训不起来": "Use fp8 for DiT base weights, greatly reducing VRAM usage. Must be enabled together with fp8_scaled (both on by default); enabling only one will break training",
  "DiT 量化（qfloat8 混合精度），大幅降低显存占用": "DiT quantization (qfloat8 mixed precision), greatly reducing VRAM usage",
  "Dropout 概率": "Dropout probability",
  "EMA 衰减率": "EMA decay rate",
  "Euler 调度器离散流位移": "Euler scheduler discrete flow shift",
  "FLUX 模型类型 (支持 Chroma)": "FLUX model type (supports Chroma)",
  "Fast 训练变体：标准 LoRA 或 T-LoRA（按扩散时间步动态调整有效 rank）": "Fast training variant: standard LoRA or T-LoRA (dynamically adjusts the effective rank by diffusion timestep)",
  "Flux 专用参数": "Flux-specific parameters",
  "Flux 模型路径": "Flux model path",
  "Gemma2b的系统提示": "System prompt for Gemma2b",
  "Klein DiT 底模文件（flux-2-klein-base-4b/9b.safetensors，须与训练种类匹配；切换 9B 时改路径），或 HF repo id": "Klein DiT base model file (flux-2-klein-base-4b/9b.safetensors; must match the training type — change the path when switching to 9B), or HF repo id",
  "Krea 2 DiT 模型路径（RAW 底模）": "Krea 2 DiT model path (RAW base model)",
  "Krea 2 专用参数": "Krea 2-specific parameters",
  "LLM Adapter 学习率": "LLM Adapter learning rate",
  "LoHa dropout 概率": "LoHa dropout probability",
  "LoKr 分解因子。常用 4~无穷，填写 -1 为无穷": "LoKr factorization factor. Commonly 4~infinity; enter -1 for infinity",
  "LoRA alpha": "LoRA alpha",
  "LoRA dropout": "LoRA dropout",
  "LoRA dropout 概率": "LoRA dropout probability",
  "LoRA 维度": "LoRA dimension",
  "LoRA-FA dropout 概率": "LoRA-FA dropout probability",
  "Lumina 专用参数": "Lumina-specific parameters",
  "Lumina 模型路径": "Lumina model path",
  "LyCORIS 网络算法": "LyCORIS network algorithm",
  "MLP 层学习率": "MLP layer learning rate",
  "Negative Prompt": "Negative Prompt",
  "Negative Prompt / 负面提示词。默认压制 NSFW、裸露和身体特写，适合公开预览页": "Negative Prompt. By default suppresses NSFW, nudity, and body close-ups — suitable for public preview pages",
  "Negative Prompt（Klein 通常留空）": "Negative Prompt (usually left empty for Klein)",
  "Negative Prompt（Turbo 采样时通常留空并关闭 CFG）": "Negative Prompt (usually left empty with CFG disabled for Turbo sampling)",
  "PiSSA rSVD 幂迭代次数": "PiSSA rSVD power iteration count",
  "PiSSA rSVD 过采样维度": "PiSSA rSVD oversampling dimension",
  "PiSSA 分解方式，推荐保持 rSVD 默认值": "PiSSA decomposition method; keeping the rSVD default is recommended",
  "PiSSA 模型保存为标准 LoRA 时的导出方式": "Export mode when saving a PiSSA model as standard LoRA",
  "PiSSA 额外作用于 1x1 Conv（实验性）": "PiSSA additionally applied to 1x1 Conv (experimental)",
  "Prompt": "Prompt",
  "Qwen Image VAE 模型路径": "Qwen Image VAE model path",
  "Qwen Image VAE 模型路径（Anima 训练必填）": "Qwen Image VAE model path (required for Anima training)",
  "Qwen3 文本模型路径": "Qwen3 text model path",
  "Qwen3 文本模型路径。可填写 safetensors / pt 文件，或完整本地模型目录": "Qwen3 text model path. Accepts a safetensors / pt file or a complete local model directory",
  "Qwen3 最大 token 长度": "Qwen3 maximum token length",
  "Qwen3-VL-4B 文本编码器路径": "Qwen3-VL-4B text encoder path",
  "Rectified Flow 位移": "Rectified Flow shift",
  "Rectified Flow 位移，默认 3.0": "Rectified Flow shift, default 3.0",
  "RoPE 不匹配处理模式": "RoPE mismatch handling mode",
  "SDXL 文本编码器 1 (ViT-L) 学习率": "SDXL text encoder 1 (ViT-L) learning rate",
  "SDXL 文本编码器 2 (BiG-G) 学习率": "SDXL text encoder 2 (BiG-G) learning rate",
  "SDXL 预测类型": "SDXL prediction type",
  "SDXL 预测类型：普通 SDXL 通常选 EPS；v-pred 或 RF 模型请按模型说明选择": "SDXL prediction type: choose EPS for regular SDXL; for v-pred or RF models, follow the model documentation",
  "T-LoRA dropout 概率。T-LoRA 自带 rank masking 正则化，一般保持 0 即可": "T-LoRA dropout probability. T-LoRA has built-in rank masking regularization, so keeping 0 is usually fine",
  "T-LoRA 动态 rank 调度。cosine 更平滑（推荐），linear 更激进": "T-LoRA dynamic rank schedule. cosine is smoother (recommended); linear is more aggressive",
  "T-LoRA 对 lora_down 使用正交初始化，加速收敛": "T-LoRA uses orthogonal initialization for lora_down to speed up convergence",
  "T-LoRA 最小动态 rank。决定高噪声 timestep 下的最低有效维度，建议 ≥ network_dim/8": "T-LoRA minimum dynamic rank. Determines the lowest effective dimension at high-noise timesteps; recommended ≥ network_dim/8",
  "T5 tokenizer 目录路径（可选，留空使用内置 configs/t5_old）": "T5 tokenizer directory path (optional; leave empty to use the built-in configs/t5_old)",
  "T5 最大 token 长度": "T5 maximum token length",
  "T5XXL 最大 token 长度（不填写使用自动）": "T5XXL maximum token length (auto when empty)",
  "Tag 文件扩展名": "Tag file extension",
  "Tagger 参数设置": "Tagger parameter settings",
  "Tagger 模型": "Tagger model",
  "Turbo 权重常驻内存（M1 模式），加快采样预览切换；需要设置 turbo_dit": "Keep Turbo weights resident in memory (M1 mode) to speed up sampling preview switching; requires turbo_dit",
  "Turbo 蒸馏 DiT 路径（可选）。填写后训练中采样预览使用 Turbo 调度，训练仍在 RAW 上进行。与 blocks_to_swap 互斥": "Turbo distilled DiT path (optional). When set, training-time sampling previews use the Turbo schedule while training still runs on RAW. Mutually exclusive with blocks_to_swap",
  "U-Net 学习率": "U-Net learning rate",
  "U-Net 的 Decoder 层分层学习率权重，共 12 层": "Per-layer learning rate weights for the U-Net Decoder layers, 12 layers total",
  "U-Net 的 Encoder 层分层学习率权重，共 12 层": "Per-layer learning rate weights for the U-Net Encoder layers, 12 layers total",
  "U-Net 的 Mid 层分层学习率权重，共 1 层": "Per-layer learning rate weight for the U-Net Mid layer, 1 layer total",
  "VAE 模型路径（Qwen-Image VAE）": "VAE model path (Qwen-Image VAE)",
  "VAE 编码/解码分块大小（需为偶数）": "VAE encode/decode tile size (must be even)",
  "VeRA dropout 概率": "VeRA dropout probability",
  "Windows 下建议关闭": "Recommended to disable on Windows",
  "Windows 下建议关闭，避免 DataLoader worker 反复导入 xformers 并刷 Triton 提示": "Recommended to disable on Windows to avoid DataLoader workers repeatedly importing xformers and flooding Triton messages",
  "arb 桶不放大图片": "ARB buckets do not upscale images",
  "arb 桶分辨率划分单位": "ARB bucket resolution division unit",
  "arb 桶分辨率划分单位，FLUX 需大于 64": "ARB bucket resolution division unit; must be greater than 64 for FLUX",
  "arb 桶分辨率划分单位，SDXL 可以使用 32": "ARB bucket resolution division unit; SDXL can use 32",
  "arb 桶分辨率划分单位，SDXL 可以使用 32 (SDXL低于32时失效)": "ARB bucket resolution division unit; SDXL can use 32 (fails below 32 for SDXL)",
  "arb 桶最大分辨率": "ARB bucket maximum resolution",
  "arb 桶最大分辨率；留空时按训练分辨率自动设置，也可以手动填写更大的值": "ARB bucket maximum resolution; auto-set from the training resolution when empty, or enter a larger value manually",
  "arb 桶最小分辨率": "ARB bucket minimum resolution",
  "caption 丢弃概率（0-1）": "Caption dropout probability (0-1)",
  "caption 后缀": "Caption suffix",
  "caption 选项": "Caption options",
  "caption（Tag）选项": "Caption (tag) options",
  "clip_l 模型文件路径": "clip_l model file path",
  "dropout 概率": "Dropout probability",
  "dropout 概率 （与 lycoris 不兼容，需要用 lycoris 自带的）": "Dropout probability (incompatible with LyCORIS; use LyCORIS's own dropout)",
  "dropout 概率。推荐 0~0.5，LoHa/LoKr/(IA)^3暂不支持": "Dropout probability. Recommended 0~0.5; not yet supported by LoHa/LoKr/(IA)^3",
  "fp8 scaled（动态缩放）。须与 fp8_base 同时开启（默认都开）；只开一个会训不起来": "fp8 scaled (dynamic scaling). Must be enabled together with fp8_base (both on by default); enabling only one will break training",
  "gemma2 模型文件路径": "gemma2 model file path",
  "logit-normal 分布的均值": "Mean of the logit-normal distribution",
  "logit-normal 分布的标准差": "Standard deviation of the logit-normal distribution",
  "logit_normal 权重策略的均值": "Mean of the logit_normal weighting strategy",
  "logit_normal 权重策略的标准差": "Standard deviation of the logit_normal weighting strategy",
  "mode 权重策略的缩放系数": "Scaling factor of the mode weighting strategy",
  "module dropout 概率": "Module dropout probability",
  "rank dropout 概率": "Rank dropout probability",
  "sigmoid 时间步采样缩放": "Sigmoid timestep sampling scale",
  "sigmoid 缩放": "Sigmoid scale",
  "t5xxl 模型文件路径": "t5xxl model file path",
  "v-parameterization 学习": "v-parameterization learning",
  "vae 编码批量大小": "VAE encoding batch size",
  "wandb 的 api 密钥": "wandb API key",
  "wandb 的 api 密钥（log_with 选 wandb 时必填）": "wandb API key (required when log_with is set to wandb)",
  "⚠ 选用 EmoSens 时请将学习率手动调至约 1.0（LoRA），优化器通过 emoPulse 自主生成 LR": "⚠ When using EmoSens, manually set the learning rate to about 1.0 (LoRA); the optimizer generates the LR autonomously via emoPulse",
  "不使用半精度 VAE": "Do not use half-precision VAE",
  "不使用半精度 VAE，当出现 NaN detected in latents 报错时使用": "Do not use half-precision VAE; use this when you hit the \"NaN detected in latents\" error",
  "不放大较小图片；max bucket 仍需覆盖训练分辨率": "Do not upscale smaller images; the max bucket must still cover the training resolution",
  "丢弃全部标签的概率，对一个图片概率不使用 caption 或 class token": "Probability of dropping all tags, i.e. not using caption or class token for an image",
  "交叉注意力层学习率": "Cross-attention layer learning rate",
  "交换到内存的 DiT block 数量以节省显存（与 turbo_dit 互斥）": "Number of DiT blocks swapped to RAM to save VRAM (mutually exclusive with turbo_dit)",
  "仅 sd-dreambooth 可用。在第 N 步时，停止训练文本编码器。设置为 -1 不训练文本编码器": "Only available for sd-dreambooth. Stop training the text encoder at step N. Set to -1 to never train the text encoder",
  "仅保存最后 n epoch 的训练状态": "Only keep training states from the last n epochs",
  "仅对 DiT / U-Net 使用 FP8 精度": "Use FP8 precision only for DiT / U-Net",
  "仅对 DiT 使用 FP8 精度": "Use FP8 precision only for DiT",
  "仅对 U-Net 使用 FP8 精度（CLIP-L不使用）": "Use FP8 precision only for U-Net (not CLIP-L)",
  "仅训练 DiT / U-Net": "Train only DiT / U-Net",
  "仅训练 Qwen3 文本编码器": "Train only the Qwen3 text encoder",
  "仅训练 U-Net": "Train only U-Net",
  "仅训练 U-Net 训练SDXL Lora时推荐开启": "Train only U-Net; recommended when training SDXL LoRA",
  "仅训练文本编码器": "Train only the text encoder",
  "从保存状态继续训练": "Resume training from a saved state",
  "从已有 LoRA / LoKr 模型继续训练，填写路径": "Resume training from an existing LoRA / LoKr model; enter the path",
  "从已有 LoRA 模型继续训练": "Resume training from an existing LoRA model",
  "从已有 network_weights 自动推断 rank / dim": "Automatically infer rank / dim from existing network_weights",
  "从已有的 LoRA 模型上继续训练，填写路径": "Resume training on top of an existing LoRA model; enter the path",
  "从某个 `save_state` 保存的中断状态继续训练，填写文件路径": "Resume training from an interrupted state saved by `save_state`; enter the file path",
  "优先读取同名 JSON 标签文件": "Prefer reading the JSON tag file with the same name",
  "优先读取同名 JSON 标签文件；适合 Anima 结构化标签流程": "Prefer reading the JSON tag file with the same name; suits the Anima structured tagging workflow",
  "优化器设置": "Optimizer settings",
  "优化器（仅 anima_lora Fast 已支持的选项）": "Optimizer (only options already supported by anima_lora Fast)",
  "低内存模式 该模式下会将 U-net、文本编码器、VAE 直接加载到显存中": "Low-memory mode: loads U-Net, text encoder, and VAE directly into VRAM",
  "低显存模式（量化时更省显存但更慢）": "Low VRAM mode (saves more VRAM when quantized, but slower)",
  "使用 CP 分解": "Use CP decomposition",
  "使用 CPU RAM activation offload 兜底省显存": "Use CPU RAM activation offload as a fallback to save VRAM",
  "使用 CPU RAM activation offload 兜底省显存；不能与 blocks_to_swap / cpu_offload_checkpointing 同时使用": "Use CPU RAM activation offload as a fallback to save VRAM; cannot be used together with blocks_to_swap / cpu_offload_checkpointing",
  "使用余弦最优传输配对 latent 和噪声": "Use cosine optimal transport to pair latents with noise",
  "使用全矩阵模式（高风险）。开启后系统会自动启用保守稳定性护栏：可训练权重保持 FP32，并默认 scale_weight_norms=1": "Use full-matrix mode (high risk). When enabled, the system automatically applies conservative stability guardrails: trainable weights stay in FP32 and scale_weight_norms=1 by default",
  "使用可学习缩放系数": "Use learnable scaling factors",
  "使用已预处理的 VAE latent cache；开启前必须完成 preprocess": "Use the preprocessed VAE latent cache; preprocess must complete before enabling",
  "使用已预处理的文本编码 cache；开启前必须完成 preprocess": "Use the preprocessed text-encoding cache; preprocess must complete before enabling",
  "使用带权重的 token，不推荐与 shuffle_caption 一同开启": "Use weighted tokens; not recommended together with shuffle_caption",
  "使用空格代替下划线": "Replace underscores with spaces",
  "保存训练状态": "Save training state",
  "保存训练状态 配合 `resume` 参数可以继续从某个状态训练": "Save training state; combined with the `resume` parameter, training can continue from a given state",
  "保存设置": "Save settings",
  "保留 tokens 时使用的分隔符": "Separator used when keeping tokens",
  "保留加载训练集的 worker，减少每个 epoch 之间的停顿。": "Keep dataset loader workers alive to reduce pauses between epochs.",
  "保留加载训练集的worker，减少每个 epoch 之间的停顿。": "Keep dataset loader workers alive to reduce pauses between epochs.",
  "保留最近 N 个中间存档": "Keep the most recent N intermediate checkpoints",
  "其他设置": "Other settings",
  "分层学习率置 0 阈值": "Threshold for zeroing per-layer learning rates",
  "分布式训练": "Distributed training",
  "分布式训练超时时间": "Distributed training timeout",
  "分辨率偏移基准像素数，默认 1024*1024": "Base pixel count for resolution shift, default 1024*1024",
  "单独的 LLM Adapter 权重路径（可选）。填写后会覆盖主模型内置 Adapter": "Separate LLM Adapter weights path (optional). Overrides the Adapter built into the main model when set",
  "卷积层 Alpha。通常等于 conv_dim 或 conv_dim/2": "Conv layer alpha. Usually equals conv_dim or conv_dim/2",
  "卷积层维度。不填则跳过卷积层训练": "Conv layer dimension. Leave empty to skip conv layer training",
  "原始训练图片目录（含子文件夹与 .txt caption；与 Kohya 相同结构）": "Original training image directory (includes subfolders and .txt captions; same structure as Kohya)",
  "合并入底模的 LoRA 权重，一行一个数字": "LoRA weights to merge into the base model, one number per line",
  "合并入底模的 LoRA 路径，一行一个路径": "LoRA paths to merge into the base model, one path per line",
  "同时分解输入与输出维度": "Factorize both input and output dimensions",
  "启用 Anima 详细诊断日志": "Enable verbose Anima diagnostic logging",
  "启用 DoRA 权重分解": "Enable DoRA weight decomposition",
  "启用 EMA 平滑（更稳但更慢）": "Enable EMA smoothing (more stable but slower)",
  "启用 Flash Attention（需要额外安装）": "Enable Flash Attention (requires extra installation)",
  "启用 PiSSA 初始化（实验性，仅 LoRA 生效）": "Enable PiSSA initialization (experimental, LoRA only)",
  "启用 QLyCORIS bypass 模式": "Enable QLyCORIS bypass mode",
  "启用 SageAttention（需要额外安装）": "Enable SageAttention (requires extra installation)",
  "启用 arb 桶": "Enable ARB buckets",
  "启用 arb 桶以允许非固定宽高比的图片": "Enable ARB buckets to allow images with non-fixed aspect ratios",
  "启用 sdpa": "Enable SDPA",
  "启用 torch.compile": "Enable torch.compile",
  "启用 xformers": "Enable xformers",
  "启用分层学习率训练（只支持网络模块 networks.lora）": "Enable per-layer learning rate training (only supports the networks.lora module)",
  "启用分辨率相关的时间步偏移": "Enable resolution-dependent timestep shift",
  "启用基础权重（差异炼丹）": "Enable base weights (difference-based training)",
  "启用对比流匹配 (Delta FM) 目标": "Enable contrastive flow matching (Delta FM) objective",
  "启用训练中采样预览图": "Enable sampling preview images during training",
  "启用训练预览图": "Enable training preview images",
  "启用训练预览图。默认关闭；开启后采样会额外加载 VAE/Qwen3，增加显存占用与训练耗时": "Enable training preview images. Off by default; when on, sampling additionally loads VAE/Qwen3, increasing VRAM usage and training time",
  "噪声设置": "Noise settings",
  "回退读取的 Tag 文件扩展名": "Fallback tag file extension",
  "固定的时间步偏移比率，留空则不使用固定偏移": "Fixed timestep shift ratio; leave empty to not use a fixed shift",
  "图像编辑参考图目录（可选，多个目录按同名文件与训练图配对）": "Reference image directory for image editing (optional; multiple directories pair with training images by matching filenames)",
  "图像翻转": "Image flip",
  "图片文件夹路径": "Image folder path",
  "在 CPU/GPU 间交换的 Transformer block 数": "Number of Transformer blocks swapped between CPU/GPU",
  "在 CPU/GPU 间交换的 Transformer block 数，用于进一步省显存": "Number of Transformer blocks swapped between CPU/GPU to further save VRAM",
  "在训练中添加噪声偏移来改良生成非常暗或者非常亮的图像，如果启用推荐为 0.1": "Add a noise offset during training to improve generation of very dark or very bright images; recommended 0.1 if enabled",
  "在随机打乱 tokens 时，保留前 N 个不变": "Keep the first N tokens unchanged when shuffling tokens",
  "多分辨率（金字塔）噪声迭代次数 推荐 6-10。无法与 noise_offset 一同启用": "Multiresolution (pyramid) noise iterations; recommended 6-10. Cannot be enabled together with noise_offset",
  "多分辨率（金字塔）衰减率 推荐 0.3-0.8，须同时与上方参数 multires_noise_iterations 一同启用": "Multiresolution (pyramid) decay rate; recommended 0.3-0.8. Must be enabled together with multires_noise_iterations above",
  "学习率": "Learning rate",
  "学习率与优化器设置": "Learning rate and optimizer settings",
  "学习率调度器设置": "Learning rate scheduler settings",
  "学习率预热步数": "Learning rate warmup steps",
  "完全使用 BF16 精度": "Use full BF16 precision",
  "完全使用 BF16 精度 仅支持 SDXL": "Use full BF16 precision; SDXL only",
  "完全使用 BF16 精度训练可训练权重": "Train trainable weights in full BF16 precision",
  "完全使用 BF16 精度训练可训练权重。Anima 一般不建议开启；Automagic / CAME 会自动禁用以降低 loss=nan 风险": "Train trainable weights in full BF16 precision. Generally not recommended for Anima; Automagic / CAME automatically disable it to reduce loss=nan risk",
  "完全使用 FP16 精度": "Use full FP16 precision",
  "完全使用 FP16 精度训练可训练权重": "Train trainable weights in full FP16 precision",
  "完全使用 FP16 精度训练可训练权重。Anima 一般不建议开启；Automagic / CAME 会自动禁用以降低 loss=nan 风险。fp16 不是 NaN 绕过方案，支持 bf16 的显卡会自动改回 bf16": "Train trainable weights in full FP16 precision. Generally not recommended for Anima; Automagic / CAME automatically disable it to reduce loss=nan risk. fp16 is not a NaN workaround — GPUs that support bf16 are automatically switched back to bf16",
  "实验性显存兜底项：梯度检查点时将部分张量卸载到 CPU": "Experimental VRAM fallback: offload some tensors to CPU during gradient checkpointing",
  "对 T5-XXL 编码器和 FLUX double块 应用注意力掩码": "Apply attention masks to the T5-XXL encoder and FLUX double blocks",
  "对 rank dropout 进行缩放补偿": "Apply scaling compensation for rank dropout",
  "对基础模型使用 FP8 精度": "Use FP8 precision for the base model",
  "将 latent 缓存写入磁盘": "Write latent cache to disk",
  "将文本编码器缓存写入磁盘": "Write text encoder cache to disk",
  "将结果中的括号进行转义处理": "Escape brackets in the results",
  "层级 offload（进一步省显存，显著变慢）": "Layer-wise offload (saves more VRAM, significantly slower)",
  "常用 `4~无穷`（填写 -1 为无穷）": "Common values: `4~infinity` (enter -1 for infinity)",
  "常用值：等于 network_dim 或 network_dim*1/2": "Common values: equal to network_dim or network_dim*1/2",
  "常用值：等于 network_dim 或 network_dim*1/2 或 1。使用较小的 alpha 需要提升学习率": "Common values: equal to network_dim, network_dim*1/2, or 1. A smaller alpha requires a higher learning rate",
  "常用值：等于 network_dim 或 network_dim*1/2 或 1。使用较小的 alpha 需要提升学习率。": "Common values: equal to network_dim, network_dim*1/2, or 1. A smaller alpha requires a higher learning rate.",
  "常用值：等于 network_dim 或 network_dim/2 或 1。T-LoRA 建议等于 network_dim": "Common values: equal to network_dim, network_dim/2, or 1. T-LoRA recommends equal to network_dim",
  "底模为 sd2.0 以后的版本需要启用": "Required when the base model is SD 2.0 or later",
  "底模文件路径": "Base model file path",
  "总学习率, 在分开设置 U-Net 与文本编码器学习率后这个值失效。": "Overall learning rate; becomes ineffective once U-Net and text encoder learning rates are set separately.",
  "批量大小": "Batch size",
  "批量大小, 越高显存占用越高": "Batch size; higher values use more VRAM",
  "批量大小，越高显存占用越高": "Batch size; higher values use more VRAM",
  "拆分 attention 计算以降低显存占用": "Split attention computation to reduce VRAM usage",
  "拆分 attention 计算以降低显存占用，通常会牺牲训练速度": "Split attention computation to reduce VRAM usage, usually at the cost of training speed",
  "按 free-fit bucket 动态编译序列长度；开启 torch.compile 时必须启用": "Dynamically compile sequence lengths per free-fit bucket; required when torch.compile is enabled",
  "按标签随机丢弃 tag 的概率": "Probability of randomly dropping tags by tag",
  "按逗号分隔的标签来随机丢弃 tag 的概率": "Probability of randomly dropping tags split by comma",
  "损失函数类型": "Loss function type",
  "推理步数": "Inference steps",
  "推理步数。Anima 官方建议 30-50": "Inference steps. Anima officially recommends 30-50",
  "推理步数（Anima 建议 30–50）": "Inference steps (Anima recommends 30–50)",
  "数据加载器进程数": "Number of data loader processes",
  "数据增强": "Data augmentation",
  "数据集设置": "Dataset settings",
  "数据集重复次数": "Dataset repeats",
  "数据集重复次数（默认 1）": "Dataset repeats (default 1)",
  "文本编码器学习率": "Text encoder learning rate",
  "文本编码器目录（Qwen3-4B；训练种类选 9B 时改为 Qwen3-8B 目录）。可在下方「训练用模型」区下载": "Text encoder directory (Qwen3-4B; switch to the Qwen3-8B directory when the training type is 9B). Downloadable in the \"Training Models\" section below",
  "文本编码器缓存批量大小": "Text encoder cache batch size",
  "文本编码器（Qwen3）量化": "Text encoder (Qwen3) quantization",
  "日志与监控": "Logging and monitoring",
  "日志保存文件夹": "Log save folder",
  "日志前缀": "Log prefix",
  "日志模块": "Logging module",
  "日志目录": "Log directory",
  "日志设置": "Logging settings",
  "日志追踪器名称": "Log tracker name",
  "时间步分布权重策略": "Timestep distribution weighting strategy",
  "时间步采样": "Timestep sampling",
  "时间步采样。默认使用 Anima 推荐的 shift": "Timestep sampling. Defaults to the Anima-recommended shift",
  "时间步采样分布": "Timestep sampling distribution",
  "是否使用 Flash Attention": "Whether to use Flash Attention",
  "显存与精度设置": "VRAM and precision settings",
  "显存与精度设置（AI Toolkit 专有）": "VRAM and precision settings (AI Toolkit only)",
  "显示 Anima 调试选项": "Show Anima debug options",
  "显示 Anima 调试选项。普通训练通常不需要开启": "Show Anima debug options. Usually not needed for regular training",
  "最大 token 长度": "Maximum token length",
  "最大时间步（0-1000，留空不限制）": "Maximum timestep (0-1000; leave empty for no limit)",
  "最大范数正则化。如果使用，推荐为 1": "Max norm regularization. Recommended 1 if used",
  "最大训练 epoch（轮数）": "Maximum training epochs",
  "最大训练 epoch；设置后 Anima 会按 epoch 和 dataloader 长度重算 step": "Maximum training epochs; once set, Anima recomputes steps from epochs and dataloader length",
  "最大训练 step；仅在 max_train_epochs 为空时按 step 控制": "Maximum training steps; step-based control only applies when max_train_epochs is empty",
  "最大训练步数（AI Toolkit 无 epoch 概念，一般 500-4000）": "Maximum training steps (AI Toolkit has no epoch concept; typically 500-4000)",
  "最大训练步数（设置了 epoch 时由 epoch 推导，可不填）": "Maximum training steps (derived from epochs when set; can be left empty)",
  "最小信噪比伽马值, 如果启用推荐为 5": "Min-SNR gamma value; recommended 5 if enabled",
  "最小信噪比伽马值，如果启用推荐为 5": "Min-SNR gamma value; recommended 5 if enabled",
  "最小时间步（0-1000，留空不限制）": "Minimum timestep (0-1000; leave empty for no limit)",
  "梯度检查点": "Gradient checkpointing",
  "梯度检查点时将部分张量卸载到 CPU": "Offload some tensors to CPU during gradient checkpointing",
  "梯度检查点（全量微调强烈建议开启）": "Gradient checkpointing (strongly recommended for full fine-tuning)",
  "梯度检查点（省显存）": "Gradient checkpointing (saves VRAM)",
  "梯度累加步数": "Gradient accumulation steps",
  "梯度裁剪阈值（0 或不填走上游默认 1.0）": "Gradient clipping threshold (0 or empty uses the upstream default 1.0)",
  "梯度裁剪阈值，0 为不裁剪": "Gradient clipping threshold; 0 disables clipping",
  "模型下载加速源（国内网络建议启用）": "Model download acceleration source (recommended for networks in mainland China)",
  "模型保存名称（Next Trainer · Krea2 默认；建议按角色/风格自行改名）": "Model save name (Next Trainer · Krea2 default; rename it per character/style as needed)",
  "模型保存名称（Next Trainer 默认；建议按角色/风格自行改名）": "Model save name (Next Trainer default; rename it per character/style as needed)",
  "模型保存名称（建议按角色/风格自行改名）": "Model save name (rename it per character/style as needed)",
  "模型保存文件夹": "Model save folder",
  "模型保存格式": "Model save format",
  "模型保存精度": "Model save precision",
  "模型预测类型": "Model prediction type",
  "正则化 - 先验损失权重": "Regularization - prior loss weight",
  "正则化数据集路径。默认留空，不使用正则化图像": "Regularization dataset path. Empty by default — no regularization images are used",
  "每 N epoch（轮）自动保存一次模型": "Automatically save the model every N epochs",
  "每 N 个 epoch 丢弃全部标签": "Drop all tags every N epochs",
  "每 N 个 epoch 生成一次预览图": "Generate a preview image every N epochs",
  "每 N 个优化 step 输出一次耗时聚合日志": "Emit an aggregated timing log every N optimizer steps",
  "每 N 个优化 step 输出一次耗时聚合日志。0 表示关闭": "Emit an aggregated timing log every N optimizer steps. 0 disables it",
  "每 N 个训练 step 检查一次 NaN": "Check for NaN every N training steps",
  "每 N 个训练 step 检查一次 NaN。0 表示自动决定": "Check for NaN every N training steps. 0 means decide automatically",
  "每 N 步生成一次预览图": "Generate a preview image every N steps",
  "每 N 步自动保存一次模型": "Automatically save the model every N steps",
  "每 N 步自动保存一次模型（与 save_every_n_epochs 二选一即可）": "Automatically save the model every N steps (choose either this or save_every_n_epochs)",
  "注意力分拆计算，省显存但更慢": "Split attention computation; saves VRAM but slower",
  "混合精度, RTX30系列以后也可以指定`bf16`": "Mixed precision; RTX 30 series and later can also specify `bf16`",
  "添加 AI 模型标签": "Add AI model tags",
  "添加等级标签": "Add rating tags",
  "生成预览图所用采样器": "Sampler used for preview image generation",
  "禁用 safetensors 的 mmap 加载": "Disable mmap loading for safetensors",
  "禁用 token 填充（与 Diffusers 的旧 Dreambooth 脚本一致）": "Disable token padding (consistent with the legacy Diffusers DreamBooth script)",
  "禁用内部 VAE 缓存机制": "Disable the internal VAE caching mechanism",
  "种子": "Seed",
  "缓存 VAE latent，全量微调推荐开启以节省显存": "Cache VAE latents; recommended for full fine-tuning to save VRAM",
  "缓存图像 latent": "Cache image latents",
  "缓存图像 latent 到磁盘": "Cache image latents to disk",
  "缓存图像 latent, 缓存 VAE 输出以减少 VRAM 使用": "Cache image latents; cache VAE outputs to reduce VRAM usage",
  "缓存文本编码器的输出到磁盘": "Cache text encoder outputs to disk",
  "缓存文本编码器的输出，减少显存使用。使用时需要关闭 shuffle_caption": "Cache text encoder outputs to reduce VRAM usage. Requires shuffle_caption to be disabled",
  "缓存文本编码器输出（需关闭 shuffle_caption）": "Cache text encoder outputs (requires shuffle_caption disabled)",
  "缩放 v-prediction 损失（与v-parameterization配合使用）": "Scale the v-prediction loss (use together with v-parameterization)",
  "缩放 v-prediction 损失（仅 v-pred 模型需要时启用）": "Scale the v-prediction loss (enable only when needed by v-pred models)",
  "网络维度（rank），Krea 2 官方推荐 32": "Network dimension (rank); Krea 2 officially recommends 32",
  "网络维度（rank），对应 AI Toolkit network.linear": "Network dimension (rank); corresponds to AI Toolkit network.linear",
  "网络维度，常用 4~128，不是越大越好, 低dim可以降低显存占用": "Network dimension; commonly 4~128 — bigger is not always better, and lower dims reduce VRAM usage",
  "网络维度，常用 4~128，不是越大越好。T-LoRA 建议 32（动态 rank 需要更大基础维度）": "Network dimension; commonly 4~128 — bigger is not always better. T-LoRA recommends 32 (dynamic rank needs a larger base dimension)",
  "网络设置": "Network settings",
  "自定义 network_args，一行一个": "Custom network_args, one per line",
  "自定义 network_args，一行一个。例如 exclude_patterns=['.*\\.mlp\\..*'] 只训练注意力层": "Custom network_args, one per line. E.g. exclude_patterns=['.*\\.mlp\\..*'] trains only attention layers",
  "自定义 optimizer_args，一行一个": "Custom optimizer_args, one per line",
  "自定义 optimizer_args，一行一个。同名参数会覆盖默认值（预览中可能显示重复，实际训练以此处为准）": "Custom optimizer_args, one per line. Same-named parameters override defaults (the preview may show duplicates; training uses the values here)",
  "自定义 optimizer_args，一行一个，例如 weight_decay=0.01": "Custom optimizer_args, one per line, e.g. weight_decay=0.01",
  "自注意力层学习率（留空=与基础 LR 相同；0=冻结该组件）": "Self-attention layer learning rate (empty = same as base LR; 0 = freeze this component)",
  "若已经存在识别的 Tag 文件，则": "If a recognized tag file already exists, then",
  "角色名称识别阈值（仅 cl_tagger 生效）": "Character name recognition threshold (cl_tagger only)",
  "触发词（可选）。填写后会追加到不含该词的 caption 开头": "Trigger word (optional). Prepended to captions that don't already contain it",
  "训练 Norm 层，不支持 (IA)^3。Anima LoKr 会自动禁用 train_norm 以避免 LyCORIS NormModule 采样崩溃。": "Train Norm layers; (IA)^3 not supported. Anima LoKr automatically disables train_norm to avoid LyCORIS NormModule sampling crashes.",
  "训练 T5XXL（不推荐）": "Train T5XXL (not recommended)",
  "训练图片分辨率": "Training image resolution",
  "训练图片分辨率，宽x高。AI Toolkit 按长边取单分辨率并自动分桶": "Training image resolution, width x height. AI Toolkit takes a single resolution by the long edge and buckets automatically",
  "训练图片分辨率，宽x高。支持非正方形，但必须是 16 倍数。": "Training image resolution, width x height. Non-square supported, but must be a multiple of 16.",
  "训练图片分辨率，宽x高。支持非正方形，但必须是 64 倍数。": "Training image resolution, width x height. Non-square supported, but must be a multiple of 64.",
  "训练实际读取的 resized 目录。留空则自动使用 .cache/anima_fast/<数据集路径>/resized 并可复用": "The resized directory actually read during training. Leave empty to automatically use and reuse .cache/anima_fast/<dataset path>/resized",
  "训练开始前先采样一次（用于验证模型与提示词配置）": "Sample once before training starts (to verify the model and prompt configuration)",
  "训练开始前生成 step 0 预览图": "Generate a step 0 preview image before training starts",
  "训练开始前生成 step 0 预览图。默认开启，确保启用预览后至少能看到一张图；关闭可省去训练前的一次采样、降低显存峰值": "Generate a step 0 preview image before training starts. On by default to guarantee at least one image once previews are enabled; turning it off skips the pre-training sample and lowers peak VRAM",
  "训练开始前生成 step 0 预览图，用作未训练基线对照。建议开启": "Generate a step 0 preview image before training starts as an untrained baseline. Recommended to enable",
  "训练数据集路径": "Training dataset path",
  "训练数据集路径（子目录按 Kohya 约定命名为 重复次数_概念名）": "Training dataset path (subdirectories named per the Kohya convention: repeats_concept-name)",
  "训练时随机打乱 tokens": "Randomly shuffle tokens during training",
  "训练时随机打乱 tokens；JSON 模式下会对 appearance / tags / environment 分组打乱": "Randomly shuffle tokens during training; in JSON mode, appearance / tags / environment groups are shuffled separately",
  "训练混合精度": "Training mixed precision",
  "训练混合精度, RTX30系列以后也可以指定`bf16`": "Training mixed precision; RTX 30 series and later can also specify `bf16`",
  "训练混合精度（Krea 2 仅支持 bf16）": "Training mixed precision (Krea 2 supports bf16 only)",
  "训练用模型": "Training models",
  "训练相关参数": "Training-related parameters",
  "训练种类": "Training type",
  "训练种类（FLUX.2 Klein base 4B / 9B，AI Toolkit 引擎）": "Training type (FLUX.2 Klein base 4B / 9B, AI Toolkit engine)",
  "训练网络模块": "Training network module",
  "训练预览图设置": "Training preview image settings",
  "调试选项": "Debug options",
  "跳过缓存完整性检查；仅用于已确认缓存完整的高级场景": "Skip cache integrity checks; only for advanced scenarios where the cache is confirmed intact",
  "迭代步数": "Iteration steps",
  "迭代步数（turbo_dit 采样时自动按 8 处理）": "Iteration steps (automatically treated as 8 when sampling with turbo_dit)",
  "适配器类型。常规训练建议选择 LoRA": "Adapter type. LoRA is recommended for regular training",
  "递归搜索子文件夹图片": "Recursively search subfolders for images",
  "速度优化选项": "Speed optimization options",
  "采样预览设置": "Sampling preview settings",
  "重启次数": "Restart count",
  "重归一化 CFG": "Renormalize CFG",
  "量化类型": "Quantization type",
  "阈值": "Threshold",
  "附加提示词 (逗号分隔)": "Additional prompts (comma-separated)",
  "随机剪裁": "Random crop",
  "随机打乱 caption（按逗号分隔）": "Randomly shuffle captions (comma-separated)",
  "随机打乱时保留前 N 个 token 不变": "Keep the first N tokens unchanged when shuffling",
  "随机种子": "Random seed",
  "随机选择预览图 Prompt": "Randomly select a preview image prompt",
  "随机选择预览图 Prompt；若训练集有多个子文件夹，则按文件夹名排序使用第一个子文件夹中的 .txt": "Randomly select a preview image prompt; if the training set has multiple subfolders, the .txt from the first subfolder sorted by folder name is used",
  "预览 Prompt": "Preview prompt",
  "预览 Prompt 文件路径；填写后优先于下方 positive/negative": "Preview prompt file path; takes precedence over the positive/negative fields below when set",
  "预览 Prompt。默认使用偏保守的人物半身预览；用户自定义后以后端实际提交值为准": "Preview prompt. Defaults to a conservative half-body portrait preview; once customized, the actual value submitted to the backend applies",
  "预览图 Prompt 文件路径。填写后将采用文件内的 prompt，而下方的选项将失效。": "Preview image prompt file path. When set, the prompt in the file is used and the options below become ineffective.",
  "预览图宽": "Preview image width",
  "预览图生成参数。`--n` 后方为反向提示词，`--w`宽，`--h`高，`--l`: CFG Scale，`--s`: 迭代步数，`--d`: 种子": "Preview image generation parameters. `--n` is followed by the negative prompt, `--w` width, `--h` height, `--l`: CFG Scale, `--s`: iteration steps, `--d`: seed",
  "预览图种子": "Preview image seed",
  "预览图高": "Preview image height",
  "颜色改变": "Color change",
  "额外训练带可学习权重的 Norm 层": "Additionally train Norm layers with learnable weights",
  "高级设置": "Advanced settings",
  "高级项：自定义 network_args，一行一个 key=value；这是传给 anima_lora LoRA 网络模块的参数列表，不是顶层 TOML。新手谨慎使用；Fast 仅允许 rank_dropout、module_dropout、loraplus_lr_ratio、loraplus_unet_lr_ratio、loraplus_text_encoder_lr_ratio，不支持的 key 会中止训练": "Advanced: custom network_args, one key=value per line; these are passed to the anima_lora LoRA network module, not top-level TOML. Use with caution if you're new; Fast only allows rank_dropout, module_dropout, loraplus_lr_ratio, loraplus_unet_lr_ratio, loraplus_text_encoder_lr_ratio — unsupported keys abort training",
}
