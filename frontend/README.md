# Next Trainer Frontend

Next Trainer 的前端已使用 Vue 3 重新编写，源代码现已公开，欢迎查看、修改和贡献。

本次前端重写由项目维护者与 GPT-5.6 Sol 协作完成。

Co-authored-by: GPT-5.6 Sol

## 开发

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

构建产物位于 `frontend/dist/`。仓库会直接保留该目录，发布时无需重新构建。
